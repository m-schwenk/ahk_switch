
switch(case)
{
	if case
	{
		if isLabel(case)
			return case
		else if isLabel("default")
			return "default"
	}
	return "" ; not explicitly needed but added for emphasis, signifies 'no input given'
}
